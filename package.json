{
  "name": "kalptaj-clone-root",
  "version": "1.0.0",
  "description": "Full-stack Kalptaj clone",
  "scripts": {
    "dev": "node run-dev.js",
    "install-all": "npm install --prefix backend && npm install --prefix frontend --legacy-peer-deps"
  }
}
